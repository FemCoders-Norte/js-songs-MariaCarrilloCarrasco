# 🎵 JS Songs - Entrega final

Proyecto de práctica de **JavaScript ES6** centrado en manipulación de arrays con `map`, `filter`, `sort` y `reduce`.

## ✅ Estado del proyecto

- Funciones implementadas en `src/songs.js`
- Tests completados correctamente
- Resultado verificado: **37/37 tests passing**

## 📁 Estructura principal

```text
src/
  data.js
  songs.js
tests/
  songs.test.js
```

## 🚀 Cómo ejecutarlo en local

```bash
npm install
npm test
```

> En PowerShell de Windows, si `npm` da error de políticas de ejecución, se puede usar `npm.cmd install` y `npm.cmd test`.

## 🧩 Funciones implementadas

- `getAllArtists()`
- `getSongsFromArtist()`
- `orderAlphabetically()`
- `orderByYear()`
- `songsByGenre()`
- `minutsToSeconds()`
- `getLongestSong()`

## 📌 Objetivo del ejercicio

Completar las utilidades del archivo `src/songs.js` sin modificar los tests, haciendo que el proyecto pase toda la batería de pruebas automatizadas.

## 🏁 Resultado final

Entrega preparada y subida al repositorio de GitHub con el proyecto funcionando y los tests validados.

